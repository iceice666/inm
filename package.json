{
  "name": "inm-site",
  "private": true,
  "scripts": {
    "build": "cd site && npm ci && npm run build && cd ../deck && npm ci && npm run build && cd .. && node scripts/patch-slide-dist.mjs && rm -rf site/dist/slide && cp -r deck/dist site/dist/slide"
  }
}
